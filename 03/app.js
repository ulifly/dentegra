// 3.Diseñar un algoritmo que calcule la raíz cuadrada 
//   de un valor ingresado por el usuario.

let valor = prompt("Ingrese un valor");
let raiz = Math.sqrt(valor);
alert("La raiz cuadrada de " + valor + " es " + raiz);
